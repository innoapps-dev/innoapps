name: postgres_audit_trail_creator
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dotenv: ^4.1.0
  fpdart: ^0.6.0
  inno_database:
    git:
      url: https://github.com/innoapps-dev/innoapps
      path: packages/inno_database

  

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^5.0.0
